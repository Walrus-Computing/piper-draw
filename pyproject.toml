[project]
name = "piper-draw"
version = "0.1.0"
description = "Interactively draw pipe diagrams for lattice surgery."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lassynth",
    "pytest-randomly>=4.0.1",
]

[tool.uv.sources]
lassynth = { path = "../isca24_SAT_scalpel", editable = true }
